#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <unistd.h>
#include <stdarg.h>

int getStringLength(char *string);
int _putchar(char character);

int _printf(const char *format, ...);

#endif